# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# anchored to the repo root: these are Python build outputs, and an unanchored
# `lib/` or `dist/` also swallows source directories deeper in the tree
/build/
/develop-eggs/
/dist/
/downloads/
/eggs/
/.eggs/
/lib/
/lib64/
/parts/
/sdist/
/var/
/wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
.venv/
venv/
ENV/
env/

# Environment Variables
.env

# User runtime config (personal settings — copy config.example.json to config.json)
config.json

# VS Code
.vscode/

# Project specific
temp/
tts_debug_output.txt
*.log
data/conversations/
data/memory_db/
data/memory/
data/bea.db
data/bea.db-wal
data/bea.db-shm
data/embeddings_cache/


# Frontend
node_modules/
src/web/frontend/dist/

# OS generated files
.DS_Store
Thumbs.db

# Large Models
*.onnx
*.bin

# the author's own planning file at the root — anchored, or it also matches
# docs/skills/plan.md, which is documentation the README links to
/PLAN.md

# personal planning notes, not part of the repo
docs/roadmap.md

# local editor/agent tooling
.claude/
