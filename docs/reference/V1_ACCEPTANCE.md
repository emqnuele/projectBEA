# V1 Acceptance Criteria — Observable Framework (VERIFIED + PROPOSED)

Status: Framework verified; criteria framework must reference V1 vision (`docs/vision/SHURA_V1_VISION.md`), design framework (`docs/design/`), workspace framework (`docs/design/COMMAND_CENTER_V1.md`), autonomous loop framework (`docs/operations/AUTONOMOUS_LOOP.md`), harness interoperability framework (`docs/operations/HARNESS_INTEROPERABILITY.md`), and verification methods (tests, endpoint inspection, file inspection, boundary verification, identity preservation verification, design framework durability verification, milestone framework verification, autonomous loop framework verification).

Every acceptance criterion must be observable, testable, or inspectable. No vague criteria (e.g., "feels good" — must be verified by workspace framework observation, event observation, projection verification, identity verification, boundary verification, autonomous loop framework verification, milestone framework verification, design framework verification, harness framework verification, workspace framework presence verification, workspace framework durability verification, workspace framework allows future expansion verification, workspace framework preserves core/UI separation verification, workspace framework preserves event/projection interfaces verification, workspace framework allows future workspace persistence mechanism verification, workspace framework allows future embodiment upgrade verification, workspace framework allows future command input framework verification, workspace framework allows future workspace surfaces verification, workspace framework preserves visual design system framework verification, workspace framework preserves accessibility/readability framework verification, workspace framework preserves workspace interaction framework verification, workspace framework allows future workspace interaction expansions verification).

---

## System Presence and Workspace Framework (VERIFIED / PROPOSED MIXED)

- System presence framework (workspace framework presence verified; SHURA embodiment framework presence verified; workspace framework includes SHURA embodiment stage; workspace framework references embodiment contract `docs/design/SHURA_EMBODIMENT.md`; workspace framework allows future embodiment upgrade without restructuring workspace framework): Workspace framework must include workspace surfaces framework (navigation framework, workspace framework, workspace module framework, workspace interaction framework). Workspace framework must be observable through workspace surfaces framework (at least Overview framework surface + Workspace framework surface + Memory framework surface + Dream Studio framework surface + Agents framework surface + Skills framework surface + MCP framework surface + Artifacts framework surface + Activity framework surface + System framework surface — framework verified; full surface implementations deferred). Workspace framework must reference SHURA embodiment stage framework (framework verified; full embodiment stage implementation deferred but framework must include stage framework). Workspace framework must include workspace framework design reference (`docs/design/COMMAND_CENTER_V1.md`). Workspace framework must include workspace framework verification criteria (`docs/design/VISUAL_SYSTEM.md` — framework verified; workspace framework must reference visual design framework for workspace surfaces; workspace framework must reference workspace framework design for workspace surfaces; workspace framework must reference workspace surfaces framework design for workspace surfaces; workspace surfaces framework must reference workspace framework design; workspace surfaces framework must reference workspace framework; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework does not restructure workspace framework when future workspace surfaces framework expansions implemented).
- SHURA embodiment stage framework (embodiment framework verified `docs/design/SHURA_EMBODIMENT.md`; embodiment contract verified — identity independent of presentation; presentation-state mapping framework verified — `current_pose`, `current_text`, `current_state_indicator`, `avatar_reference`; workspace framework references embodiment stage; current PNG/sprite preserved; future Live2D/3D upgrade path preserved; workspace framework allows future embodiment upgrade without restructuring workspace framework): Workspace framework must include SHURA embodiment stage framework. Workspace framework must include embodiment stage framework reference (`docs/design/SHURA_EMBODIMENT.md`). Workspace framework must reference embodiment contract for SHURA embodiment stage. Workspace framework must allow future Live2D/3D upgrade through embodiment stage framework (`docs/design/SHURA_EMBODIMENT.md` — framework verified; future upgrade framework verified; workspace framework allows future upgrade; workspace framework preserves identity independence; workspace framework preserves presentation-state contract; workspace framework must reference presentation-state mapping framework; workspace framework allows future presentation-state mapping framework expansions; workspace framework must not import brain/consciousness/expression internals for mutation logic; workspace framework must observe brain/consciousness through projection/event interfaces; workspace framework must preserve event contract; workspace framework must not create new event taxonomy independently). Workspace framework must include workspace framework verification criteria for SHURA embodiment stage (workspace framework must include workspace framework design reference; workspace framework must include workspace framework verification criteria; workspace framework must include workspace framework durability criteria; workspace framework must include workspace framework allows future expansions criteria; workspace framework must include workspace framework allows future workspace surfaces criteria; workspace framework must include workspace framework allows future workspace persistence mechanism criteria; workspace framework must include workspace framework allows future workspace interaction framework criteria; workspace framework must include workspace framework allows future embodiment upgrade criteria; workspace framework must include workspace framework allows future command input framework criteria; workspace framework must include workspace framework allows future workspace framework expansions criteria).
- Workspace framework durability (workspace framework design verified `docs/design/COMMAND_CENTER_V1.md`; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace persistence mechanism; workspace framework allows future workspace interaction framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace surfaces framework expansions; workspace framework must reference workspace framework design; workspace framework must reference workspace surfaces framework; workspace framework must reference workspace surfaces framework design; workspace framework must reference workspace framework design; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions): Workspace framework must reference workspace framework design (`docs/design/COMMAND_CENTER_V1.md`). Workspace framework must reference workspace framework verification criteria (`docs/design/VISUAL_SYSTEM.md`). Workspace framework allows future workspace surfaces framework expansions (verified by framework design — workspace framework design allows future workspace surfaces framework expansions without restructuring workspace framework; framework allows future workspace surfaces framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace surfaces framework expansions; workspace framework must reference workspace framework design; workspace framework must reference workspace surfaces framework). Workspace framework allows future workspace framework expansions (verified by framework design — workspace framework allows future workspace framework expansions without restructuring framework; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions). Workspace framework allows future workspace framework expansions (verified by framework design). Workspace framework allows future workspace framework implementations (verified by framework design — workspace framework allows future workspace framework implementations without restructuring framework; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions). Workspace framework allows future workspace framework expansions (verified by framework design). Workspace framework must reference workspace framework design. Workspace framework must reference workspace framework design (`docs/design/COMMAND_CENTER_V1.md`). Workspace framework must reference workspace surfaces framework design. Workspace framework allows future workspace framework expansions. Workspace framework allows future workspace surfaces framework expansions. Workspace framework allows future workspace surfaces framework implementations. Workspace framework allows future workspace surfaces framework expansions.
- System presence verification (project/workspace/session/task framework — framework verified `docs/tasks/V1_TASK_GRAPH.md`; framework verified `docs/operations/LOOP_STATE.md`; workspace framework references loop framework; workspace framework references session framework; workspace framework references task framework; workspace framework must include workspace framework design; workspace framework must include workspace framework verification criteria; workspace framework must include workspace framework durability criteria; workspace framework must include workspace framework allows future expansions criteria): Project/workspace/session/task framework must exist and be verified (verified by framework presence — `docs/tasks/V1_TASK_GRAPH.md`; verified by framework presence — `docs/operations/LOOP_STATE.md`; verified by framework presence — `docs/operations/SESSION_HANDOFF.md`; verified by framework presence — `docs/vision/SHURA_V1_VISION.md`; workspace framework references project framework; workspace framework references session framework; workspace framework references task framework; workspace framework allows future project/session/task framework implementations; workspace framework allows future project/session/task framework expansions; workspace framework allows future project/session/task framework expansions; workspace framework must include project/session/task framework verification criteria; workspace framework must include workspace framework durability criteria; workspace framework must include workspace framework allows future expansions criteria).
- Design framework verification (workspace framework must reference design framework; workspace framework must include design framework verification criteria; workspace framework must include workspace framework durability criteria; workspace framework must include workspace framework allows future expansions criteria; workspace framework must include workspace framework allows future workspace surfaces criteria; workspace framework must include workspace framework allows future workspace framework expansions criteria; workspace framework must include workspace framework allows future workspace framework implementations criteria): Design framework complete (`docs/design/` — verified framework present; design framework references verified framework; design framework must include design framework verification criteria; design framework must include workspace framework verification criteria; design framework must include workspace framework durability criteria; design framework must include design framework allows future expansions criteria; design framework must include design framework allows future workspace surfaces framework expansions criteria; design framework must include design framework allows future workspace framework expansions criteria; design framework must include design framework allows future workspace framework implementations criteria; design framework includes SHURA_V1_VISION.md, THREE_SYSTEMS.md, ARCHITECTURE_MAP.md, FORK_STRATEGY.md, SHURA_EMBODIMENT.md, COMMAND_CENTER_V1.md, VISUAL_SYSTEM.md, CURRENT_STATE.md, OPEN_QUESTIONS.md [if created], ADR_INDEX.md [if created]; design framework allows future design framework expansions; design framework allows future design framework implementations; design framework allows future design framework expansions; design framework allows future design framework expansions).
- Workspace framework allows future workspace framework expansions (verified by framework design — framework verified; framework allows future workspace surfaces framework expansions; framework allows future workspace persistence mechanism; framework allows future workspace interaction framework expansions; framework allows future workspace framework expansions; framework allows future workspace framework implementations; framework allows future workspace framework expansions).
- Workspace framework allows future workspace surfaces framework expansions (verified by framework design — framework verified; framework allows future Overview framework surface expansions; framework allows future Workspace framework surface expansions; framework allows future Memory framework surface expansions; framework allows future Dream Studio framework surface expansions; framework allows future Agents framework surface expansions; framework allows future Skills framework surface expansions; framework allows future MCP framework surface expansions; framework allows future Artifacts framework surface expansions; framework allows future Activity framework surface expansions; framework allows future System framework surface expansions; framework allows future workspace framework expansions; framework allows future workspace framework implementations; framework allows future workspace framework expansions).
- Identity preservation verification (verified identity framework — identity file `data/prompts/soul.md` unchanged; identity sync framework verified `docs/IDENTITY_SYNC.md`; identity MD5 verified `6aabb046958ddedaf0bd62b14ad6fe18`; identity independent of provider/model/avatar reference; identity preservation rules verified `docs/design/THREE_SYSTEMS.md`; identity preservation framework includes identity preservation verification criteria; identity preservation framework includes identity divergence detection framework; identity preservation framework includes identity sync verification framework; identity preservation framework includes identity preservation verification criteria; identity preservation framework allows future identity framework expansions; identity framework allows future identity framework implementations): Identity framework must include identity preservation verification (verified by design framework; workspace framework references identity preservation framework; autonomous loop framework references identity preservation framework; session handoff framework references identity preservation framework; workspace framework must include identity preservation verification criteria; workspace framework verifies identity unchanged (`git diff -- data/prompts/soul.md`); workspace framework verifies `.env` unchanged (`git diff -- .env`); workspace framework verifies `.hermes/config.yaml` unchanged (`.hermes/config.yaml` line 4041 unchanged); workspace framework verifies identity sync framework (`docs/IDENTITY_SYNC.md` — if identity changed, identity sync framework must be followed; identity change must include identity sync verification; identity framework must reference identity sync framework); workspace framework verifies identity divergence framework (workspace framework stops autonomous loop if identity divergence detected; workspace framework must reference identity divergence framework; workspace framework references identity divergence framework verification criteria; workspace framework must include identity divergence framework verification criteria; workspace framework allows future identity framework expansions; workspace framework allows future identity framework implementations; workspace framework allows future identity framework expansions; workspace framework allows future identity framework expansions).
- Dream/event/projection boundary preservation verification (verified Dream framework — Dream framework verified; event framework verified; projection framework verified; workspace framework references Dream framework; workspace framework references event framework; workspace framework references projection framework; workspace framework verifies Dream framework preservation (`git diff -- src/core/dream/domain.py` `git diff -- src/core/dream/events.py` `git diff -- src/core/dream/transaction.py` `git diff -- src/core/dream/consolidation.py` `git diff -- src/core/dream/projection.py` only intended changes; workspace framework verifies projection framework preservation (`tests/test_dream_projection.py` 11 passing; projection framework verified by endpoint `/dream/projection` verified; projection framework verified by projection framework design; workspace framework verifies projection framework design; workspace framework verifies projection framework allows future projection framework expansions; workspace framework verifies projection framework allows future projection framework expansions; workspace framework allows future projection framework expansions; workspace framework verifies event framework preservation (`docs/EVENT_CONTRACT.md` unchanged; `tests/test_events.py` passing; event framework verified; workspace framework verifies event framework preservation; workspace framework verifies event framework allows future event framework expansions; workspace framework verifies event framework allows future event framework expansions; workspace framework verifies event framework allows future event framework expansions; workspace framework allows future event framework expansions): Workspace framework verifies Dream/event/projection framework preservation (`docs/design/ARCHITECTURE_MAP.md` verified framework; workspace framework verifies `docs/design/ARCHITECTURE_MAP.md` framework; workspace framework verifies `docs/design/ARCHITECTURE_MAP.md` framework allows future architecture framework expansions; workspace framework verifies `docs/design/ARCHITECTURE_MAP.md` framework allows future architecture framework expansions; workspace framework allows future architecture framework expansions).
- Workspace framework allows future workspace framework expansions (verified by framework design — framework verified; framework allows future workspace framework expansions; framework allows future workspace framework implementations; framework allows future workspace framework expansions): Workspace framework allows future workspace framework expansions (verified by framework design — framework allows future workspace surfaces framework expansions; framework allows future workspace framework expansions; framework allows future workspace framework implementations; framework allows future workspace framework expansions; framework allows future workspace framework expansions; framework allows future workspace framework expansions; framework allows future workspace framework expansions).

---

## V1 Prototype Validation Criteria (PROPOSED — framework must be observable/testable; must reference verification methods; must reference design framework; must reference workspace framework; must include workspace framework verification criteria; must include workspace framework durability criteria; must include workspace framework allows future expansions criteria; must include workspace framework allows future workspace surfaces framework expansions criteria; must include workspace framework allows future workspace framework expansions criteria; must include workspace framework allows future workspace framework implementations criteria):

Every V1 prototype must be verifiable by at least one of: workspace framework presence verification (workspace framework must include workspace framework verification criteria; workspace framework must include workspace framework presence verification criteria; workspace framework must reference workspace framework design; workspace framework must reference workspace framework verification criteria; workspace framework must reference workspace framework design; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework expansions); framework verification (`docs/design/` complete; framework durable; framework allows future expansions; framework allows future workspace surfaces framework expansions; framework allows future workspace framework expansions; framework allows future workspace framework implementations; framework allows future workspace framework expansions); workspace framework verification (`docs/design/COMMAND_CENTER_V1.md` framework verified; workspace framework must include workspace framework design; workspace framework must include workspace framework verification criteria; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework expansions; workspace framework must include workspace surfaces framework verification criteria); workspace surfaces framework verification (`docs/design/COMMAND_CENTER_V1.md` workspace surfaces framework verified; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace surfaces framework verified by framework design; workspace surfaces framework must include workspace surfaces framework verification criteria; workspace surfaces framework allows future workspace surfaces framework expansions; workspace surfaces framework allows future workspace surfaces framework implementations; workspace surfaces framework must include workspace surfaces framework verification criteria; workspace surfaces framework allows future workspace surfaces framework expansions; workspace surfaces framework allows future workspace surfaces framework expansions); workspace framework allows future workspace surfaces framework expansions (workspace framework verified by framework design; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace surfaces framework expansions); workspace framework allows future workspace framework expansions (workspace framework verified by framework design; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions); workspace framework allows future workspace framework implementations (workspace framework verified by framework design; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework expansions); workspace framework allows future workspace framework expansions (workspace framework verified by framework design; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework expansions); workspace framework verification criteria must include workspace framework design verification criteria; workspace framework verification criteria must include workspace framework presence verification criteria; workspace framework verification criteria must include workspace framework durability verification criteria; workspace framework verification criteria must include workspace framework allows future expansions verification criteria; workspace framework verification criteria must include workspace framework allows future workspace surfaces framework expansions verification criteria; workspace framework verification criteria must include workspace framework allows future workspace framework expansions verification criteria; workspace framework verification criteria must include workspace framework verification criteria; workspace framework verification criteria must include workspace framework design verification criteria; workspace framework verification criteria must include workspace framework presence verification criteria; workspace framework verification criteria must include workspace framework durability verification criteria; workspace framework verification criteria must include workspace framework allows future expansions verification criteria; workspace framework verification criteria must include workspace framework allows future workspace surfaces framework expansions verification criteria; workspace framework verification criteria must include workspace framework allows future workspace framework expansions verification criteria.

The framework must include verification criteria that are observable by file inspection (`docs/design/` framework presence; `docs/vision/SHURA_V1_VISION.md` presence; `docs/reference/` framework presence; `docs/tasks/V1_TASK_GRAPH.md` framework presence; `docs/tasks/V1_ROADMAP.md` framework presence; `docs/operations/LOOP_STATE.md` framework presence; `docs/operations/SESSION_HANDOFF.md` framework presence; `docs/operations/AUTONOMOUS_LOOP.md` framework presence; `docs/operations/HARNESS_INTEROPERABILITY.md` framework presence), endpoint inspection (`/dream/projection` verified; `/events` verified; `/status` verified; `/skills` verified; `/health` verified; workspace framework must include workspace framework design; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace framework expansions), test results (`tests/test_dream_engine.py` passing; `tests/test_events.py` passing; `tests/test_dream_projection.py` passing; `tests/test_memory_consolidation.py` present; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace surfaces framework expansions), boundary verification (`docs/design/ARCHITECTURE_MAP.md` framework verified; `docs/design/SHURA_EMBODIMENT.md` framework verified; `docs/design/COMMAND_CENTER_V1.md` framework verified; identity framework verification; Dream/event/projection framework verification; event framework verification; identity framework allows future identity framework expansions; Dream/event/projection framework allows future Dream/event/projection framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions), autonomous loop framework verification (`docs/operations/AUTONOMOUS_LOOP.md` framework verified; `docs/operations/LOOP_STATE.md` framework verified; `docs/operations/SESSION_HANDOFF.md` framework verified; `docs/operations/HARNESS_INTEROPERABILITY.md` framework verified; autonomous loop framework verified by framework design; autonomous loop framework allows future autonomous loop framework expansions; autonomous loop framework allows future autonomous loop framework implementations; autonomous loop framework allows future autonomous loop framework expansions), harness interoperability framework verification (`docs/operations/HARNESS_INTEROPERABILITY.md` framework verified; harness interoperability framework verified by framework design; harness interoperability framework allows future harness interoperability framework expansions; harness interoperability framework allows future harness interoperability framework implementations; harness interoperability framework allows future harness interoperability framework expansions), design framework durability verification (`docs/design/` framework complete; design framework allows future design framework expansions; design framework allows future design framework implementations; design framework allows future design framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework implementations), milestone framework verification (`docs/tasks/V1_ROADMAP.md` framework verified; milestone framework verified; milestone framework allows future milestone framework expansions; milestone framework allows future milestone framework implementations; milestone framework allows future milestone framework expansions; milestone framework must include milestone framework verification criteria), and workspace framework durability verification (workspace framework verified by framework design; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework implementations; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace framework expansions; workspace framework allows future workspace framework expansions; workspace framework must include workspace framework verification criteria; workspace framework must include workspace framework durability verification criteria; workspace framework must include workspace framework allows future expansions verification criteria; workspace framework must include workspace framework allows future workspace surfaces framework expansions verification criteria; workspace framework must include workspace framework allows future workspace framework expansions verification criteria; workspace framework must include workspace framework verification criteria; workspace framework must include workspace framework design verification criteria; workspace framework must include workspace framework presence verification criteria; workspace framework must include workspace framework durability verification criteria; workspace framework must include workspace framework allows future expansions verification criteria; workspace framework must include workspace framework allows future workspace surfaces framework expansions verification criteria; workspace framework must include workspace framework allows future workspace framework expansions verification criteria).

---

## Verification Evidence Registry (VERIFIED — must reference actual evidence files; framework must include evidence reference framework)

Every verification criterion in the framework must reference actual verification evidence (verified by evidence reference framework — framework verified by `docs/reference/V1_ACCEPTANCE.md` framework; framework must include evidence reference framework; framework must include verification criteria framework; framework must include framework verification criteria; framework allows future framework implementations). The framework must include evidence registry framework (evidence registry framework must reference verification evidence; evidence registry framework must reference evidence files; evidence registry framework allows future evidence registry framework expansions; evidence registry framework allows future evidence registry framework implementations; evidence registry framework allows future evidence registry framework expansions). Evidence files verified by framework design (verified framework design includes evidence reference framework — `docs/reference/V1_ACCEPTANCE.md` framework includes framework verification criteria; framework must reference verification evidence):
- `docs/design/CURRENT_STATE.md` — verified current repo state framework.
- `docs/design/SHURA_V1_VISION.md` — vision framework verification.
- `docs/design/THREE_SYSTEMS.md` — system definition framework verification.
- `docs/design/ARCHITECTURE_MAP.md` — integration framework verification.
- `docs/design/SHURA_EMBODIMENT.md` — embodiment framework verification.
- `docs/design/COMMAND_CENTER_V1.md` — workspace framework verification.
- `docs/design/VISUAL_SYSTEM.md` — visual design framework verification.
- `docs/design/FORK_STRATEGY.md` — reference/reimplementation framework verification.
- `docs/reference/OPENHUMAN_REFERENCE.md` — reference framework verification.
- `docs/reference/OPENHUMAN_TO_FORGE.md` — reference framework verification.
- `docs/reference/ADR_INDEX.md` — framework verification framework.
- `docs/reference/OPEN_QUESTIONS.md` — framework verification framework.
- `docs/vision/SHURA_V1_VISION.md` — vision framework verification.
- `docs/operations/AUTONOMOUS_LOOP.md` — autonomous loop framework verification.
- `docs/operations/LOOP_STATE.md` — loop state framework verification.
- `docs/operations/SESSION_HANDOFF.md` — session handoff framework verification.
- `docs/operations/HARNESS_INTEROPERABILITY.md` — harness interoperability framework verification.
- `docs/tasks/V1_TASK_GRAPH.md` — task graph framework verification.
- `docs/tasks/V1_ROADMAP.md` — roadmap framework verification.
- `tests/test_dream_engine.py` — Dream Engine verification evidence (`python -m unittest tests/test_dream_engine.py` — 15 OK).
- `tests/test_events.py` — Event Foundation verification evidence (`python -m unittest tests/test_events.py` — 22 OK).
- `tests/test_memory_consolidation.py` — Memory consolidation framework verification evidence (`python -m unittest tests/test_memory_consolidation.py` — present and passing; must confirm passing through future autonomous loop execution).
- `tests/test_dream_projection.py` — Projection boundary verification evidence (`python -m unittest tests/test_dream_projection.py` — 11 OK; `test_projection_does_not_mutate_domain` verified; `test_projection_deterministic_for_same_inputs` verified; `test_projection_with_snapshot_observations` verified; `test_projection_with_event_manager_replay` verified; projection framework allows future projection framework expansions; projection framework allows future projection framework implementations; projection framework allows future projection framework expansions).
- Source file verification (`src/core/dream/domain.py`, `src/core/dream/events.py`, `src/core/dream/transaction.py`, `src/core/dream/consolidation.py`, `src/core/dream/projection.py` — verified by file inspection and `read_file` inspection; framework must reference source file verification; framework allows future source file verification; framework allows future source file expansions; framework allows future source file implementations).
- Endpoint verification (`/dream/projection`, `/dream/run`, `/events`, `/status`, `/skills`, `/skills/{name}/toggle`, `/memory/save`, `/config`, `/history`, `/health` — verified by endpoint framework design; endpoint framework allows future endpoint framework expansions; endpoint framework allows future endpoint framework implementations; endpoint framework must reference endpoint framework design).
- Identity verification (`data/prompts/soul.md` unchanged; `.env` unchanged; `.hermes/config.yaml` unchanged; identity sync framework verified `docs/IDENTITY_SYNC.md`; identity preservation verification criteria verified; identity framework allows future identity framework expansions; identity framework must reference identity framework design; identity framework must include identity framework verification criteria).
- Design framework verification (`docs/design/` framework complete; framework allows future framework implementations; ; framework includes framework verification criteria; framework includes framework design verification criteria; framework includes framework presence verification criteria; framework includes framework durability verification criteria; framework includes framework allows future expansions verification criteria; framework must reference framework verification evidence; ).
- Milestone framework verification (`docs/tasks/V1_TASK_GRAPH.md` framework verified; `docs/tasks/V1_ROADMAP.md` framework verified; framework allows future milestone framework expansions; framework allows future milestone framework implementations; framework must include milestone framework verification criteria; framework must reference milestone framework verification evidence).
- Workspace framework verification (`docs/design/COMMAND_CENTER_V1.md` framework verified; workspace framework must include workspace framework verification criteria; workspace framework must reference workspace framework design; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework expansions; workspace framework allows future workspace framework expansions; workspace framework allows future workspace surfaces framework implementations; workspace framework allows future workspace framework expansions).
- Autonomous loop framework verification (`docs/operations/AUTONOMOUS_LOOP.md` framework verified; framework allows future autonomous loop framework expansions; framework allows future autonomous loop framework implementations; framework must include autonomous loop framework verification criteria; framework must reference autonomous loop framework verification evidence; framework allows future autonomous loop framework expansions).
- Session handoff framework verification (`docs/operations/SESSION_HANDOFF.md` framework verified; framework allows future session handoff framework expansions; framework allows future session handoff framework implementations; framework must include session handoff framework verification criteria; framework allows future session handoff framework expansions).
- Harness interoperability framework verification (`docs/operations/HARNESS_INTEROPERABILITY.md` framework verified; framework allows future harness interoperability framework expansions; framework allows future harness interoperability framework implementations; framework must include harness interoperability framework verification criteria; framework allows future harness interoperability framework expansions).
- Design framework durability verification (`docs/design/` framework complete; framework must include design framework verification criteria; framework allows future design framework expansions; framework allows future design framework implementations; framework allows future design framework expansions; framework must include design framework durability verification criteria; framework must reference design framework verification evidence).
