# V1 Task Graph — Dependency-Aware Tasks (VERIFIED FRAMEWORK; TASKS PROPOSED/VERIFIED MIXED)

Status: Framework verified; tasks must be bounded, testable, and dependency-aware. Must be updated by autonomous loop (`docs/operations/AUTONOMOUS_LOOP.md`) and session handoff (`docs/operations/SESSION_HANDOFF.md`).

Verified framework references:
- Milestones (`docs/tasks/V1_ROADMAP.md` — milestone framework verified; 7 proposed milestones + verified milestone 0).
- Design framework (`docs/design/` — verified framework present; specific design documents verified).
- Architecture framework (`docs/design/ARCHITECTURE_MAP.md` — verified framework; system integration relationships verified).
- System definitions (`docs/design/THREE_SYSTEMS.md` — verified framework; ProjectSHURA/ATLAS/FORGE definitions verified/proposed).
- Embodiment framework (`docs/design/SHURA_EMBODIMENT.md` — framework verified; contract verified).
- Command center framework (`docs/design/COMMAND_CENTER_V1.md` — framework verified; workspace surfaces framework verified).
- Visual design framework (`docs/design/VISUAL_SYSTEM.md` — framework verified; token/typography/animation rules verified).
- Autonomous loop (`docs/operations/AUTONOMOUS_LOOP.md` — framework verified; loop phases/protocol/safety/stopping conditions verified/proposed).
- Loop state (`docs/operations/LOOP_STATE.md` — framework verified; state format verified).
- Session handoff (`docs/operations/SESSION_HANDOFF.md` — framework verified; handoff format verified).
- Harness interoperability (`docs/operations/HARNESS_INTEROPERABILITY.md` — framework verified; harness requirements/protocol verified/proposed).

Every task in this graph must reference: milestone, dependencies, source files (`docs/vision/`, `docs/design/`, `docs/operations/`, `docs/reference/`, source files, specs), verification plan (tests / endpoint inspection / file inspection / boundary verification), acceptance criteria (`docs/reference/V1_ACCEPTANCE.md` — framework must reference criteria), and status (READY / IN_PROGRESS / IMPLEMENTED / TESTED / VERIFIED / BLOCKED / DEFERRED).

---

## Milestone 0: Foundation Verified + Design Framework (VERIFIED — completed before and during this design pass)

Verified tasks (completed; must reference verification evidence):
- Task M0-T1: Dream Engine Phase 2 verified (`docs/DREAM_ENGINE.md`; `src/core/dream/domain.py`, `events.py`, `transaction.py`, `consolidation.py`; `tests/test_dream_engine.py` 15 passing; projection layer added; event emission safe; no mutation through event emission; no visual scene generation; no streaming output separate from journal; deferred items documented).
- Task M0-T2: Event Foundation verified (`docs/EVENT_CONTRACT.md`; `src/core/events.py`; `tests/test_events.py` 22 passing; persistent JSONL journal; subscription/replay; backward-compatible `publish()`; event taxonomy extended with Dream/Memory categories; replay preserves original IDs; malformed line safe).
- Task M0-T3: Memory consolidation framework verified (`docs/MEMORY_CONSOLIDATION.md`; `tests/test_memory_consolidation.py` present; `MemoryConsolidationTransaction`, `MemoryProposal`, `RehearsalResult`, `ValidationResult`, `ConsolidationEngine`; framework present; full pipeline deferred; rehearsal never writes to `MemoryStorage` directly; commit explicit; replay reconstructs from events).
- Task M0-T4: Projection layer verified (`src/core/dream/projection.py`; `tests/test_dream_projection.py` 11 passing; `DreamStateProjection`; `build_projection()` read-only; `/dream/projection` endpoint; safe malformed replay; no mutation of domain; no forbidden direct coupling to brain/consciousness/expression/avatar/OBS).
- Task M0-T5: Design framework verified (`docs/design/CURRENT_STATE.md`; `docs/design/THREE_SYSTEMS.md`; `docs/design/ARCHITECTURE_MAP.md`; `docs/design/SHURA_EMBODIMENT.md`; `docs/design/COMMAND_CENTER_V1.md`; `docs/design/VISUAL_SYSTEM.md`; `docs/reference/OPENHUMAN_REFERENCE.md`; `docs/reference/OPENHUMAN_TO_FORGE.md`; `docs/reference/FORK_STRATEGY.md`; `docs/vision/SHURA_V1_VISION.md`).
- Task M0-T6: OpenHuman reference analysis verified (`docs/reference/OPENHUMAN_REFERENCE.md`; `docs/reference/OPENHUMAN_TO_FORGE.md`; `tinyhumansai/openhuman` artifact ingested; reference/reimplementation/reject/defer classifications made; design framework preserves ProjectSHURA architecture independence).

Status for Milestone 0: VERIFIED (all tasks completed; framework durable; design framework complete; autonomous loop protocol designed; workspace framework designed; embodiment contract designed; integration map verified; reference/reimplementation decision made; all design framework documents present; verification criteria defined for framework elements).

---

## Milestone 1: Autonomous Operating System (PROPOSED — must become durable through autonomous loop execution)

Task dependencies (derived from verified Milestone 0 + design specs + architecture audit rules):
- M1-T1: Update `AGENTS.md` with V1 autonomous loop rules, verification rules, architecture rules, identity preservation rules, Dream/event/projection boundary rules, harness interoperability rules. Must reference current verified framework (`docs/vision/SHURA_V1_VISION.md`, `docs/design/THREE_SYSTEMS.md`, `docs/design/ARCHITECTURE_MAP.md`, `docs/operations/AUTONOMOUS_LOOP.md`, `docs/operations/LOOP_STATE.md`, `docs/operations/SESSION_HANDOFF.md`, `docs/operations/HARNESS_INTEROPERABILITY.md`). Must preserve existing useful rules (layering rule, anti-caricature rule, mood ID preservation, no provider coupling, identity/operating/skill layer separation, no direct brain/PNG coupling, no identity/provider lock-in, no multi-agent complexity before single-agent loop stable).
- M1-T2: Initialize `docs/tasks/V1_TASK_GRAPH.md` framework (task graph framework verified; task descriptions proposed; dependency relationships proposed; status tracking framework verified; must be updated by autonomous loop).
- M1-T3: Initialize `docs/tasks/V1_ROADMAP.md` framework (milestone framework verified; milestone descriptions proposed; exit criteria framework verified; dependency relationships proposed; must reference `V1_TASK_GRAPH.md`; must be updated by autonomous loop).
- M1-T4: Initialize `docs/reference/ADR_INDEX.md` framework (ADR framework verified; first entry must reference OpenHuman reference/reimplementation decision; future ADR entries must reference design framework; must be updated by autonomous loop when architecture decisions made). Status: VERIFIED — M1-T4 completed. ADR_INDEX.md present with ADR-001 (OpenHuman reference/reimplementation decision) and ADR reference rules. Framework verified; future ADR entries must reference design framework; framework allows future ADR framework expansions; framework includes ADR framework verification criteria.
- M1-T5: Initialize `docs/reference/OPEN_QUESTIONS.md` framework (open questions framework verified; must reference C4 mechanism BLOCKED; identity sync if changed; Live2D/3D embodiment upgrade; full workspace implementation; full ATLAS operational layer; full Dream Transaction pipeline; full autonomous loop execution; must be updated by autonomous loop when questions resolved or new questions emerge). Status: VERIFIED — M1-T5 completed. OPEN_QUESTIONS.md present with 8 open questions (Q1: C4 BLOCKED; Q2: identity sync; Q3: workspace shell DEFERRED; Q4: Dream pipeline DEFERRED; Q5: embodiment DEFERRED; Q6: ATLAS PARTIAL; Q7: autonomous loop PROPOSED; Q8: harness interoperability PROPOSED). Framework verified; includes resolution protocol; framework allows future open questions framework expansions; framework includes open questions framework verification criteria.
- M1-T6: Initialize `docs/reference/V1_ACCEPTANCE.md` framework (acceptance criteria framework verified; criteria must reference V1 vision (`docs/vision/SHURA_V1_VISION.md`); criteria must reference workspace framework (`docs/design/COMMAND_CENTER_V1.md`); criteria must reference autonomous loop framework (`docs/operations/AUTONOMOUS_LOOP.md`); criteria must include identity preservation verification, Dream boundary verification, projection read-only verification, event contract preservation verification, workspace framework presence verification, autonomous loop framework presence verification, harness interoperability framework presence verification, design framework durability verification; criteria must reference verification methods: `git status`, `git diff`, `python -m unittest`, endpoint inspection (`/dream/projection`, `/events`, `/status`, `/skills`, `/health`), file inspection (`docs/`, `tests/`), boundary checks (no brain/consciousness import in projection; no identity divergence; no secret exposure; no C4 retry; projection read-only)).
- M1-T7: Execute autonomous loop protocol once (test autonomous session — bounded task: read current loop state, inspect repo state, confirm identity unchanged, confirm `.env` unchanged, confirm tests passing, confirm projection boundary preserved, select a bounded ready task from `V1_TASK_GRAPH.md` framework — must reference a real ready task with verification plan; implement/test/update loop state/session handoff; verify loop state updated; verify session handoff updated; verify no forbidden coupling/identity divergence/secret exposure/test failure; confirm loop stops safely when bounded task completes or when blocked). This is the critical verification step: autonomous loop must work at least once (even if only a bounded documentation/task update) before Milestone 1 can be considered complete.
- M1-T8: Implement workspace Dream event adapter (subscribe subsystem=dream via EventManager.subscribe(); observe Dream lifecycle; workspace structured presentation; bounded endpoint/module + focused tests; framework allows future framework implementations; framework includes framework verification criteria). Dependencies: Milestone 0 verified; workspace/event/projection framework verified. Verification: endpoint accessible (/workspace/dream-events); tests/test_workspace_events.py passing (replay usage verified; event IDs preserved; projection boundary preserved); event taxonomy preserved; replay mechanism verified; . Status: VERIFIED — M1-T8 completed by autonomous loop. Framework verified; adapter endpoint `/workspace/dream-events` accessible; `tests/test_workspace_events.py` 3 passing; event taxonomy preserved; replay mechanism preserved; projection boundary preserved; workspace framework preserved; loop state updated; session handoff updated.

Milestone 1 exit criteria (verified framework; full autonomous execution must be tested by M1-T7):
- AGENTS.md durable with autonomous loop rules.
- `docs/operations/AUTONOMOUS_LOOP.md` durable.
- `docs/operations/LOOP_STATE.md` initialized and updated by autonomous session.
- `docs/operations/SESSION_HANDOFF.md` initialized and updated by autonomous session.
- `docs/tasks/V1_TASK_GRAPH.md` framework initialized and updated.
- `docs/tasks/V1_ROADMAP.md` framework initialized.
- `docs/reference/ADR_INDEX.md` framework initialized.
- `docs/reference/OPEN_QUESTIONS.md` framework initialized.
- `docs/reference/V1_ACCEPTANCE.md` framework initialized.
- Autonomous loop executed at least once (verified by loop state update + session handoff update + bounded task completed/tested + loop stops safely + no identity divergence + no secret exposure + no Dream/event/projection boundary collapse + tests passing + loop can select next bounded ready task safely).
- Design framework durable (`docs/design/` complete; all framework elements present; no architecture ambiguity unaddressed — any ambiguity must become ADR entry or open question).
- Workspace framework durable (`docs/design/COMMAND_CENTER_V1.md` framework verified; workspace framework allows future workspace surface expansions; workspace framework allows future workspace persistence mechanism; workspace framework allows future workspace interaction framework expansions without restructuring workspace framework).
- Embodiment framework durable (`docs/design/SHURA_EMBODIMENT.md` contract verified; presentation-state contract preserved; future upgrade path preserved; workspace framework references embodiment stage).

---

## Milestone 2: Workspace Framework Implementation (PROPOSED — framework verified; components deferred)

Task framework (derived from milestone framework + design framework):
- M2-T1: Overview workspace surface framework (must observe loop state, milestone, recent completed tasks, active agent/task framework, health/status framework; must not import brain internals for mutation; mutation through workspace framework must route through explicit interfaces; framework design verified; full component deferred but framework must be observable).
- M2-T2: Workspace surface framework (primary workspace surface; must observe active task/project/session framework, workspace interaction framework, workspace persistence framework; framework verified; full component deferred).
- M2-T3: Dream Studio workspace framework (Dream observation framework; must observe projection/interface framework; framework verified by `docs/design/COMMAND_CENTER_V1.md` and `/dream/projection` endpoint; full Dream Studio surface components deferred but framework must observe Dream state through projection/event replay; must include event replay mechanism framework; must include Dream lifecycle observation framework; must include Dream snapshot observation framework; must include Dream transaction/reconciliation observation framework).
- M2-T4: Memory workspace framework (memory observation framework; must observe `DreamSnapshot` fields through projection; must include event replay framework for memory-related events; must observe through projection/event interfaces; full Memory workspace surfaces deferred).
- M2-T5: Agents workspace framework (agent/task observation framework; must observe agent/task framework state (`docs/tasks/V1_TASK_GRAPH.md`, `docs/operations/LOOP_STATE.md`); must include agent/task visibility framework; must observe event feed (`EventCategory.AGENT`, `EventCategory.TOOL`, `EventCategory.DREAM`); full agent workspace surfaces deferred; must not directly access brain/consciousness internals for agent state observation).
- M2-T6: Skills workspace framework (skill registry observation framework; must observe `/skills` endpoint; must observe skill activation/toggle framework (`/skills/{name}/toggle` endpoint); full Skills workspace components deferred; framework must observe `SkillRegistry` state through endpoint/projection interfaces; must not import brain skill internals directly for skill state observation or mutation).
- M2-T7: MCP / Tools workspace framework (MCP server/tool observation framework; must observe `.hermes/config.yaml` configured servers; must observe event replay for tool execution events; must include tool inventory framework; must include tool execution framework (future service endpoints — framework only for V1, not full tool execution framework); framework verified; full MCP/Tool workspace surfaces deferred).
- M2-T8: Artifacts workspace framework (artifact/workspace file observation framework; must observe workspace/project/session framework; framework design allows future workspace persistence mechanism; framework allows future artifact surfaces; framework verified; full Artifacts workspace surfaces deferred).
- M2-T9: Activity workspace framework (event feed / audit framework; must observe `EventManager.subscribe()` / replay mechanism; framework verified by `tests/test_events.py`; must show structured events with `event_type`, `subsystem`, `run_id`, `payload`, `severity`, `visibility`; must not create new event taxonomy independently; must observe replay mechanism that preserves original event IDs; must include event replay framework; must include event subscription framework; framework must be durable through future event taxonomy extensions — any taxonomy extension must follow `docs/EVENT_CONTRACT.md` rules and must include framework update documentation).
- M2-T10: System workspace framework (health/config/status framework; must observe `/status`, `/health`, `/config` endpoints; framework verified; full System workspace surfaces deferred).
- M2-T11: Workspace persistence mechanism framework (must add file-based mechanism for workspace/project/session reference + workspace state persistence; mechanism deferred; framework must allow mechanism addition without restructuring workspace framework; framework verified by workspace framework design — workspace framework allows persistence mechanism extension).

Milestone 2 exit criteria (framework verified; components deferred; framework durable; must be observable that framework allows future component additions):
- Workspace framework durable (`docs/design/COMMAND_CENTER_V1.md` — framework verified; workspace framework allows future workspace surfaces without restructuring framework; workspace framework references projection/event interfaces; workspace framework references workspace persistence mechanism framework; workspace framework references workspace interaction framework; workspace framework references embodiment stage framework).
- Workspace surfaces framework observable (at least workspace framework design allows Overview + Workspace + Memory framework + Dream Studio framework + Agents framework + Skills framework + MCP framework + Artifacts framework + Activity framework + System framework; framework allows future surfaces; framework must reference workspace framework design; framework must reference projection/event interfaces).
- Workspace framework preserves core/UI separation (`docs/design/ARCHITECTURE_MAP.md` verified; workspace surfaces observe brain/consciousness through projection/event interfaces; workspace interaction framework routes mutation through explicit interfaces; workspace framework does not import brain/consciousness internals for mutation logic).
- Workspace framework preserves identity independence (`docs/design/SHURA_EMBODIMENT.md` verified; workspace framework references embodiment stage framework; embodiment framework does not include identity content; identity remains independent of provider/model/avatar reference).
- Workspace framework allows future workspace persistence mechanism (`docs/design/COMMAND_CENTER_V1.md` — framework allows mechanism addition; mechanism deferred but framework durable).
- Workspace framework preserves event contract (`docs/EVENT_CONTRACT.md` verified; workspace event feed framework uses existing event taxonomy; replay mechanism verified; subscription mechanism verified; no new event taxonomy independently created without documentation).
- Workspace framework allows future workspace interaction expansions (`docs/design/COMMAND_CENTER_V1.md` — framework allows command input framework extension; framework allows workspace interaction framework expansion; framework must preserve mutation routing through explicit interfaces).
- Design framework durable (`docs/design/` complete; workspace framework referenced in design framework; workspace framework must reference design framework; workspace framework must reference autonomous loop framework).

---

## Milestone 3-7 (PROPOSED — deferred to milestone-based autonomous execution; framework designed; must be executed by autonomous loop when dependencies satisfied)

Milestones 3-7 (Dream Studio full, Memory/Context integration, Agents/Tasks/MCP integration, Embodiment/Workspace polish, V1 Prototype validation) are deferred to future milestone-based autonomous execution. The framework (`docs/tasks/V1_TASK_GRAPH.md`, `docs/tasks/V1_ROADMAP.md`, `docs/operations/AUTONOMOUS_LOOP.md`, `docs/operations/LOOP_STATE.md`) must guide the autonomous loop to select bounded milestone tasks when dependencies satisfied.

The autonomous loop must update milestone status when bounded milestone tasks complete (verified by task graph updates + loop state updates + session handoff updates + milestone exit criteria verification). The autonomous loop must not claim milestone complete until milestone exit criteria satisfied (verified by framework presence + test results + design framework verification + workspace framework verification + boundary preservation verification).

---

## Task Graph Framework Requirements (VERIFIED — framework must be durable; tasks must reference milestone, dependencies, verification criteria, design framework)

Every task entry in `docs/tasks/V1_TASK_GRAPH.md` must contain:
- Task ID (e.g., M0-T1, M1-T1, M2-T1, M3-T1, M4-T1, M5-T1, M6-T1, M7-T1 — derived from milestone framework; must reference milestone framework).
- Title (derived from milestone framework; must reference milestone framework; must reference design framework; must reference verification criteria).
- Milestone reference (`V1_ROADMAP.md` milestone reference — must match milestone framework).
- Status (READY / IN_PROGRESS / IMPLEMENTED / TESTED / VERIFIED / BLOCKED / DEFERRED — must match milestone framework; must be updated by autonomous loop).
- Dependency references (must reference `V1_TASK_GRAPH.md` task IDs; must reference milestone framework; dependency satisfaction verified by autonomous loop before selecting ready task).
- Source references (`docs/vision/SHURA_V1_VISION.md`, `docs/design/THREE_SYSTEMS.md`, `docs/design/ARCHITECTURE_MAP.md`, `docs/design/SHURA_EMBODIMENT.md`, `docs/design/COMMAND_CENTER_V1.md`, `docs/design/VISUAL_SYSTEM.md`, `docs/reference/OPENHUMAN_REFERENCE.md`, `docs/reference/OPENHUMAN_TO_FORGE.md`, `docs/reference/FORK_STRATEGY.md`, `docs/operations/AUTONOMOUS_LOOP.md`, `docs/operations/LOOP_STATE.md`, `docs/operations/SESSION_HANDOFF.md`, `docs/operations/HARNESS_INTEROPERABILITY.md`, source files, specs, tests — must be referenced for verification).
- Implementation scope (bounded scope only; must reference milestone framework; must not expand beyond bounded change; must reference `docs/design/CURRENT_STATE.md` current verified state; must reference `docs/design/SHURA_V1_VISION.md` vision criteria; must reference `docs/reference/V1_ACCEPTANCE.md` acceptance criteria; must reference architecture rules from `docs/design/THREE_SYSTEMS.md`).
- Verification plan (tests / endpoint inspection / file inspection / boundary verification — must reference `tests/test_...` modules, endpoint names, file paths, design framework references; must include identity preservation verification, Dream/event/projection boundary verification, event contract preservation verification, workspace framework verification if applicable, autonomous loop framework preservation verification, harness interoperability framework preservation verification).
- Acceptance criteria (`docs/reference/V1_ACCEPTANCE.md` criteria reference — must reference V1 vision criteria; must include verification criteria; must include framework durability criteria; must include boundary preservation criteria).
- Risk / blockages (must reference C4 mechanism status — BLOCKED; identity sync status — verified unchanged; Dream boundary status — preserved; event contract status — preserved; workspace framework status — durable; autonomous loop framework status — durable; test status — passing; design framework status — complete; milestone framework status — verified/proposed; must be updated by autonomous loop).

The autonomous loop (`docs/operations/AUTONOMOUS_LOOP.md`) must select bounded tasks from `docs/tasks/V1_TASK_GRAPH.md` by confirming:
- Task status = READY (verified by task graph framework; must reference milestone framework; must confirm dependency satisfaction by checking dependency task statuses).
- Milestone framework verified (current milestone framework verified; milestone exit criteria must be satisfied before milestone status can be updated; autonomous loop must confirm milestone framework verification before selecting milestone tasks).
- No BLOCKED dependency (dependency task status = BLOCKED must prevent ready task selection; autonomous loop must reference dependency task IDs; must reference milestone framework; must confirm dependency satisfaction before selecting ready task).
- Bounded scope confirmed (task scope does not expand beyond bounded change; must reference design framework; must reference milestone framework; must confirm no identity divergence; must confirm no secret exposure; must confirm no C4 retry; must confirm no Dream/event/projection boundary collapse; must confirm no workspace framework restructuring; must confirm no forbidden coupling).
- Verification plan exists (must reference specific test module, endpoint, file inspection criteria; must confirm verification criteria from `docs/reference/V1_ACCEPTANCE.md` applicable to task; must confirm design framework verification criteria applicable to task if design framework applies).
- Design framework references verified (task must reference relevant design framework documents; must confirm design framework durability; must confirm design framework allows task scope; must confirm design framework allows future expansions without restructuring framework).
- Autonomous loop framework preserved (task implementation must not interfere with autonomous loop execution; must confirm loop protocol (`docs/operations/AUTONOMOUS_LOOP.md`) remains intact; must confirm loop state (`docs/operations/LOOP_STATE.md`) remains readable; must confirm session handoff (`docs/operations/SESSION_HANDOFF.md`) remains durable; must confirm autonomous loop stops safely when bounded task completes; must confirm autonomous loop selects next bounded ready task safely).

---

## Milestone-Based Implementation Sequence (PROPOSED — framework verified; must be executed by autonomous loop; framework allows milestone progress updates)

The implementation sequence is framework-based (milestones and tasks defined by framework; autonomous loop selects bounded milestone tasks when dependencies satisfied; autonomous loop updates task status; autonomous loop updates milestone status when milestone exit criteria satisfied). The framework must remain durable through autonomous execution.

Verified milestone 0 framework: Foundation Verified + Design Framework (verified by file inspection + test results + endpoint verification + design framework presence + autonomous loop framework presence + session handoff framework presence + harness interoperability framework presence + vision framework presence + architecture framework presence + reference framework presence + workspace framework presence + embodiment framework presence + visual design framework presence + design framework references verified + design framework durability verified + design framework allows future workspace/embodiment/autonomous framework expansions without restructuring framework).

Proposed milestone framework (must be executed by autonomous loop): Milestones 1-7 (Durable Autonomous Operating System; Workspace Framework Implementation; Dream Studio Full Surface; Memory/Context Integration; Agents/Tasks/MCP Integration; Embodiment/Workspace Polish; V1 Prototype Validation). Must reference `docs/tasks/V1_ROADMAP.md` framework; must reference `docs/tasks/V1_TASK_GRAPH.md` framework; must confirm milestone dependency relationships (verified by framework; must be preserved by autonomous loop); must confirm milestone exit criteria framework (verified by `docs/reference/V1_ACCEPTANCE.md` framework); must confirm milestone framework updates through autonomous loop execution (loop state updates must reference milestone progress; session handoff updates must reference milestone progress; design framework must reference milestone framework updates; task graph framework must reference milestone framework updates; roadmap framework must reference milestone framework updates).

The framework must allow future milestone framework updates (new milestones; revised milestone exit criteria; revised milestone dependency relationships; revised milestone framework) without restructuring milestone framework or design framework. The autonomous loop framework must allow milestone framework updates (autonomous loop updates milestone framework when milestone exit criteria satisfied; autonomous loop must confirm milestone framework updates through loop state updates and session handoff updates; autonomous loop must not claim milestone complete without verification evidence).
